[tool.poetry]
name = "temp-python-aws-app"
authors = []
description = ""
version = "0"

[tool.poetry.dependencies]
python = "^3.10, <3.12"
rasterio = "1.3.9"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
